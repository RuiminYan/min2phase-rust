# Rust
target/
**/*.rs.bk

# wasm-pack output — NOT ignored; we ship it so `git clone` + open page works.
# To rebuild: wasm-pack build crates/m2p-wasm --release --target web --out-dir ../../pkg

# Shootout stats files (regenerated by tests)
/fixtures/*_stats.txt
/fixtures/*_out.tsv
/fixtures/rust_*.tsv
/fixtures/java_*_stats.txt
/fixtures/java_*_out.tsv

# tmp
/.tmp/
*.bin

# Editor
/.idea/
/.vscode/
*.swp
*~
.DS_Store
Thumbs.db
